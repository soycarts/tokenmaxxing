/** The onboarding prompt, verbatim from SPEC.md. Shown on / and /setup. */
export const ONBOARDING_PROMPT = `Set up tokenmaxxing for me: run \`npx tokenmaxxing-cli@latest init\`, show me the report it prints,
and if I like it run \`npx tokenmaxxing-cli link\` and open the URL it prints.
Don't edit any of my tool config files unless I say so.`;

export const INSTALL_COMMAND = "npx tokenmaxxing-cli@latest init";

export const PRIVACY_LINE = "We receive hourly token counts per model. Never prompts, paths, or project names.";
